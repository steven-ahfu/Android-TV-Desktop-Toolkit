[project]
name = "android-tv-tools"
version = "4.1.3"
requires-python = ">=3.10"
dependencies = [
    "customtkinter",
    "pillow",
    "pywinstyles",
]

[dependency-groups]
dev = [
    "pyinstaller",
]

[project.scripts]
android-tv-tools = "android_tv_tools:main"
