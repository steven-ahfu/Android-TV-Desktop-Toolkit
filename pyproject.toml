[project]
name = "android-tv-desktop-toolkit"
version = "4.1.5"
requires-python = ">=3.10"
dependencies = [
    "customtkinter",
    "pillow",
    "pywinstyles",
]

[dependency-groups]
dev = [
    "pyinstaller",
    "pytest",
]

